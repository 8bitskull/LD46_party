local h = {}

h.PROXY_LOADED = hash("proxy_loaded")


h.DEFAULT = hash("default")
h.OVER = hash("over")
h.DOWN = hash("down")
h.RELEASED = hash("released")

h.TOUCH = hash("touch")

h.CLICK = hash("click")

return h